const contactComponent = () => {
  const component = document.createElement("div");
  component.id = "contact";

  component.textContent = "test contact";

  return component;
};

export { contactComponent };
